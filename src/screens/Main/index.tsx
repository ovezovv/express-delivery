import React from 'react';
import {Text, View} from 'react-native';
import styles from 'src/styles';

const MainScreen = () => {
  return (
    <View style={styles.screenContainer}>
      <Text>Приложение для сотрудника</Text>
    </View>
  );
};

export default MainScreen;
